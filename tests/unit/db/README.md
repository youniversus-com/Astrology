# Database unit tests

Exercises `AstrologySqlite` against an isolated `HOME` (see root `conftest.py`): `astrocfg` version, settings location persistence, and configuration keys.

No GTK required. Marker: `unit`.
